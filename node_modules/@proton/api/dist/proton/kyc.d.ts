import type { ApiClass } from '../index';
import type { CountryOption, ApplyForKycParams, KycApplyResponse } from '../types';
import { KycStatus } from '../types';
export declare function fetchKycCountries(this: ApiClass, { chain }: {
    chain: string;
}): Promise<CountryOption[]>;
/**
 * FAILED
 * UNSUBMITTED
 * PENDING
 * PASSED
 */
export declare function checkUserKycStatus(this: ApiClass, { chain, actor, expectedTier }: {
    chain: string;
    actor: string;
    expectedTier: number;
}): Promise<KycStatus>;
export declare function applyForKyc(this: ApiClass, params: ApplyForKycParams): Promise<KycApplyResponse>;
